# familiar Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-13

## Active Technologies
- Python 3.11 + browser-use (Playwright-based browser automation with multi-provider LLM support), Click (CLI framework), PyYAML (config parsing), rich (terminal UI), pydantic (config validation), python-dotenv (automatic .env file loading) (001-ai-e2e-testing)
- File-based (Markdown test steps, YAML suite configs, JSON/JUnit XML results output) (001-ai-e2e-testing)
- Python 3.11+ + browser-use (with native model providers), playwrigh (002-fix-langchain-browser-use-models)
- N/A (bug fix only) (002-fix-langchain-browser-use-models)
- Python 3.11+ (project supports 3.11, 3.12, 3.13) (005-github-actions-pr-checks)
- N/A (CI workflow configuration only) (005-github-actions-pr-checks)
- Python 3.11+ + browser-use>=0.9.5, click>=8.1.0, pydantic>=2.0.0, pyyaml>=6.0, rich>=13.0.0, python-dotenv>=1.0.0 (006-tech-debt-sprint)
- File system (markdown test files, YAML configs) (006-tech-debt-sprint)
- File system (markdown agent instruction files, read alongside suite files) (007-agent-instructions)

## Project Structure

```text
src/familiar/
├── cli/          # CLI commands (Click-based)
├── core/         # Core business logic
├── models/       # Data models (Pydantic)
├── formatters/   # Output formatters
├── logging/      # Logging setup
└── utils/        # Utilities (env, interpolation, browser, dotenv)
tests/
├── unit/         # Unit tests
├── integration/  # Integration tests
└── contract/     # Contract tests
```

## Commands

```bash
cd /Users/adam/Development/familiar
uv run pytest                    # Run tests
uv run ruff check src/          # Lint
uv run ruff format src/         # Format
uv run mypy src/                # Type check
```

## Code Style

Python 3.11: Follow PEP 8, use type hints, write docstrings for public APIs

## Recent Changes
- 007-agent-instructions: Added Python 3.11+ + browser-use>=0.9.5, click>=8.1.0, pydantic>=2.0.0, pyyaml>=6.0, rich>=13.0.0, python-dotenv>=1.0.0
- 006-tech-debt-sprint: Added Python 3.11+ + browser-use>=0.9.5, click>=8.1.0, pydantic>=2.0.0, pyyaml>=6.0, rich>=13.0.0, python-dotenv>=1.0.0
- 005-github-actions-pr-checks: Added Python 3.11+ (project supports 3.11, 3.12, 3.13)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
