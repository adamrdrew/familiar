# Enter Login Credentials

Enter the following credentials into the login form:
- Email/Username: ${TEST_USER}
- Password: ${TEST_PASSWORD}

Verify that the password field shows masked characters (dots or asterisks)

## Expected Outcome

Both fields should contain the entered values (password should be hidden).

